{
  "name": "App Dev (Compose, target=dev)",
  "dockerComposeFile": "docker-compose.yml",
  "service": "mumble",
  "workspaceFolder": "/home/node",
  "remoteUser": "node",
  "overrideCommand": true,

  "postCreateCommand": "bash -lc 'cd /home/node && if [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ]; then npm ci; else npm install; fi'",
  "postStartCommand": "bash -lc 'cd /home/node && node -v && npm -v'",

  "portsAttributes": {
    "8081": { "label": "App", "onAutoForward": "notify" }
  }
}
