{
  "name": "App Dev (Compose, target=dev)",
  "dockerComposeFile": "docker-compose.yml",
  "service": "mumble",
  "workspaceFolder": "/home/node",
  "remoteUser": "node",
  "overrideCommand": true,
  "postCreateCommand": "bash -lc 'cd /home/node && node -v && npm -v && npm ci && echo \"🎭 Installing Playwright browsers...\" && npx playwright install chromium && echo \"🚀 Initial build...\" && BUILD_MODE=development node build-esbuild.mjs && echo \"⚙️ Configuring gh CLI...\" && gh repo set-default Flexpair/mumbling-mole || true'",
  // Removed postStartCommand - start dev server manually to avoid system overload
  // Run: ./start-dev-server.sh when ready
  "portsAttributes": {
    "8081": {
      "label": "App",
      "onAutoForward": "notify",
      "visibility": "public"
    }
  },
  "forwardPorts": [
    8081
  ],
  "features": {
    "ghcr.io/devcontainers/features/docker-outside-of-docker": {}
  }
}